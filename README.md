

## Gradle Commands

Build and install locally:

```
./gradlew build  publishToMavenLocal
```

Compile example:

```
./gradlew :ct-command:run
```

???

```
./gradlew build :ct-gradle-example:compileAllCt
```